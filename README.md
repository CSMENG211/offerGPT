## offerGPT

A small Java project scaffold.

### Run with Maven

```sh
export JAVA_HOME=$(/usr/libexec/java_home)
mvn compile exec:java
```

### Run with `javac`

```sh
javac -d out src/Main.java
java -cp out Main
```
